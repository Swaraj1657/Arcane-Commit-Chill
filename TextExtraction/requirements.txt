# Python dependencies for TextExtraction
requests>=2.28.0
pdf2image>=1.16.0
Pillow>=9.0.0
groq>=0.3.0

# Notes:
# - `pdf2image` requires Poppler (you already set `POPPLER_PATH`).
# - If `groq` package name differs in your environment, adjust accordingly.